package com.workout.android.theme

import androidx.compose.ui.graphics.Color

// Работа — тёплый оранжевый
val WorkOrange = Color(0xFFFF6B35)
val WorkOrangeDim = Color(0x33FF6B35)

// Отдых — спокойный синий
val RestBlue = Color(0xFF4A90D9)
val RestBlueDim = Color(0x334A90D9)

// Основной акцент — тёмно-коричневый
val BrownPrimary = Color(0xFFBF8B6E)        // светлый (на тёмном фоне)
val BrownPrimaryDim = Color(0x33BF8B6E)
val BrownContainer = Color(0xFF4A2E1A)      // тёмный контейнер
val OnBrownContainer = Color(0xFFEDD5C0)    // текст поверх контейнера

// Surfaces
val Background = Color(0xFF111214)
val Surface = Color(0xFF1C1D20)
val SurfaceVariant = Color(0xFF26272C)
val OnSurface = Color(0xFFF2F2F3)
val OnSurfaceMuted = Color(0xFF8A8B90)

// Прочее
val DangerRed = Color(0xFFE53935)
